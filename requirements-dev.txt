# Зависимости для разработки и сборки.
# Установка: pip install -r requirements-dev.txt
#
# Cython и setuptools нужны для сборки .pyx в .so. Помимо них требуется
# компилятор C, который через pip не ставится:
#   Debian/Ubuntu   sudo apt install build-essential python3-dev
#   macOS           xcode-select --install
#   Windows         Build Tools for Visual Studio, компонент C++ build tools

-r requirements.txt

Cython>=3.0.0
setuptools>=42

pytest>=7.0.0
pytest-cov>=4.0

# Линтер и форматтер. Перед заливкой:
#   ruff check --fix .
#   ruff format .
ruff>=0.6

# Эталонная реализация для сверки SSIM в тестах. Без неё соответствующая
# проверка пропускается, остальные работают.
scikit-image>=0.21