# Зависимости для запуска фреймворка.
# Установка: pip install -r requirements.txt
#
# Основной способ установки — pip install -e . --no-build-isolation,
# он подтягивает то же самое из pyproject.toml и собирает Cython-расширения.
# Этот файл нужен для окружений, где пакет не устанавливается (CI, докер-слой).

numpy>=1.24
Pillow>=10.0
