# Дополнительные метрики неотличимости: LPIPS, DISTS, NIQE, BRISQUE.
# Установка: pip install -r requirements-metrics.txt
#
# Тянет torch (~2.5 ГБ). Без пакета эти четыре метрики поднимают RuntimeError
# с подсказкой, но остаются в реестре и не ломают импорт.

pyiqa>=0.1.10
